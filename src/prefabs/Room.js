// prefab for a view of a room
class Room extends Phaser.GameObjects.Sprite {
    constructor(scene, x, y, texture, roomData, hasBox) {
        super(scene, x, y, texture);

        // parse roomdata and set up everything
    }

    create() {

    }
}